generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_PRISMA")
}

enum Role {
  admin
  user
}

model Profile {
  id   String @id @db.Uuid
  role Role   @default(user)

  @@map("profile")
}
